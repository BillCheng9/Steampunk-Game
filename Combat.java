public class Combat {

    // get stats from player

    // get stats from enemy

    // update player UI

    // read player option

    // check health stats of both parties

    // make enemy pick move

    // read enemy move

    // check health stats of both parties

    // repeat

    // update player UI and stats

    public void initateCombat() {
        while ( (Player.Health > 0) && (Player.Health > 0) ) {
            // continue fight
        }
        endCombat();
    }

    public void endCombat() {
        System.out.print("GG PLACEHOLDER");
    }

}
